name: ai_fitness_coach
description: AI-powered fitness coaching app

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State Management (ADD THESE)
  flutter_riverpod: ^2.4.9
  
  # Already existing
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  
  # Functional Programming (ADD THIS)
  dartz: ^0.10.1
  
  # Utilities (if not present, ADD THESE)
  equatable: ^2.0.5
  uuid: ^4.0.0